<!DOCTYPE html>
<html lang="en">
  <head>
      <meta charset="UTF-8">
      <link rel="stylesheet" href="/style.css">
      <title>MakersBnB</title>
  </head>
  <header>
    <div class="wrap-logo">
      <a href="#logo" class="logo">Our Logo</a>
    </div>
    <nav>
      <a class="active" href="/spaces">Spaces</a>
      <a href="/requests/<%=session[:id]%>">Requests</a>
      <a href="/">Sign out</a>
    </nav>
  </header>
  <body>
    <div class="spaces-container">
      <div class="body-info">
        <h1>Book a Space</h1>
        <p>Here you can find spaces for rest</p>
      </div>
      <div>
        <form class="spaces-form" name="add_space" action="/space_form" method="GET">
          <label>You can add your space to rent!</label>
          <input type="submit" value="Add space"/>
        </form>
        <form class="spaces-form" name="dates" action="/spaces/search" method="GET">
          <label>Available from:</label>
          <input type="date" name="start_date" min='<%=@today%>'/>
          <label>Available to:</label>
          <input type="date" name="end_date" min='<%=@tomorrow%>'/>
          <input type="submit" value="Check"/>
        </form>
      </div>
      <%if @spaces.empty?%>
        <h1>Sorry</h1>
        <p>We do not have offers for these dates</p>
      <%end%>
      <% @spaces.each do |space|%>
        <div class="spaces-item">
          <div class="space-item-header">
            <a href="/spaces/<%=space.id%>"><div class="space-title"><%= space.space_name%></div></a>
            <div class="space-price"><%= space.price%>£</div>
          </div>
          <div class="space-content">
            <p class="space-describtion"><%=space.description%></p>
          </div>
        </div>
      <%end%>
    </div>
  </body>
</html>